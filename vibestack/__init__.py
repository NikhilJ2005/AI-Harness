"""VibeStack: an architecture-aware AI harness that generates self-healing
FastAPI backends from natural-language descriptions.

Phase 0 provides the project skeleton: configuration, the structured
``ProjectSpec`` and ``GenerationState`` models, an LLM client, and a
command-line entry point that parses a prompt into a ``ProjectSpec``.
"""

__version__ = "0.1.0"
